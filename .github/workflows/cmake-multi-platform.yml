# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  push:
    branches: [ "vr-sbs" ]
  pull_request:
    branches: [ "vr-sbs" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Release]
        c_compiler: [gcc, clang, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          - os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++
          - os: ubuntu-latest
            c_compiler: clang
            cpp_compiler: clang++
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-latest
            c_compiler: cl

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    # Install Vulkan SDK - required for Vulkan headers, libraries, and glslangValidator
    - name: Install Vulkan SDK
      uses: jakoch/install-vulkan-sdk-action@v1
      with:
        vulkan_version: 1.4.309.0
        install_runtime: true
        cache: true

    # Linux-specific dependencies (from README.md + OpenGL for OpenXR)
    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libx11-dev \
          libxcb1-dev \
          libxcb-keysyms1-dev \
          libxcursor-dev \
          libxi-dev \
          libxinerama-dev \
          libxrandr-dev \
          libxxf86vm-dev \
          libtbb-dev \
          libgl1-mesa-dev \
          libglu1-mesa-dev \
          ccache

    # Setup ccache for faster rebuilds (CMakeLists.txt auto-detects ccache)
    - name: Setup ccache
      uses: hendrikmuhs/ccache-action@v1
      with:
        key: ${{ matrix.os }}-${{ matrix.c_compiler }}-${{ matrix.build_type }}
        max-size: 500M
        variant: ${{ runner.os == 'Windows' && 'sccache' || 'ccache' }}

    # Cache CMake _deps folder (Slang, DLSS, OpenXR downloads)
    - name: Cache CMake dependencies
      uses: actions/cache@v4
      with:
        path: ${{ github.workspace }}/build/_deps
        key: cmake-deps-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'nvpro_core2/cmake/FindSlang.cmake', 'nvpro_core2/cmake/FindNGX.cmake') }}

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      # OpenXR SDK is fetched via FetchContent in CMakeLists.txt if not found
      # Disable features that require NVIDIA-specific dependencies for CI
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -DDISABLE_DEFAULT_SCENE=ON
        -DDISABLE_MESH_ASSETS_DOWNLOAD=ON
        -S ${{ github.workspace }}

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config ${{ matrix.build_type }}

    # Prepare artifacts for upload (Windows only for now - primary target)
    - name: Prepare Windows artifacts
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $artifactDir = "${{ github.workspace }}/artifact"
        New-Item -ItemType Directory -Force -Path $artifactDir
        # Copy executable and required DLLs
        Copy-Item "${{ steps.strings.outputs.build-output-dir }}/${{ matrix.build_type }}/*.exe" $artifactDir -Force
        Copy-Item "${{ steps.strings.outputs.build-output-dir }}/${{ matrix.build_type }}/*.dll" $artifactDir -Force -ErrorAction SilentlyContinue
        # Copy shaders
        if (Test-Path "${{ steps.strings.outputs.build-output-dir }}/${{ matrix.build_type }}/shaders") {
          Copy-Item "${{ steps.strings.outputs.build-output-dir }}/${{ matrix.build_type }}/shaders" $artifactDir -Recurse -Force
        }
        # Copy nvshaders
        if (Test-Path "${{ steps.strings.outputs.build-output-dir }}/${{ matrix.build_type }}/nvshaders") {
          Copy-Item "${{ steps.strings.outputs.build-output-dir }}/${{ matrix.build_type }}/nvshaders" $artifactDir -Recurse -Force
        }

    - name: Upload build artifacts
      if: runner.os == 'Windows'
      uses: actions/upload-artifact@v4
      with:
        name: vk_gaussian_splatting-${{ matrix.os }}-${{ matrix.build_type }}
        path: ${{ github.workspace }}/artifact
        retention-days: 14

  # Create a pre-release with all artifacts
  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/vr-sbs'
    permissions:
      contents: write
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Create zip archives
      run: |
        cd artifacts
        for dir in */; do
          zip -r "${dir%/}.zip" "$dir"
        done

    - name: Update pre-release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: dev-build
        name: Development Build
        body: |
          🚧 **Automated development build from vr-sbs branch**
          
          Commit: ${{ github.sha }}
          
          ⚠️ This is an unstable pre-release build. Use at your own risk.
          
          Requires:
          - NVIDIA GPU with Vulkan 1.4 support
          - Latest NVIDIA drivers for DLSS-RR
          - OpenXR runtime for VR features
        prerelease: true
        files: artifacts/*.zip
        generate_release_notes: false
